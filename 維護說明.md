# 給接手維護這個網站的人（AI 或工程師）看

這份不是給老師看的教學內容，是給**之後要繼續補內容的人**看的技術筆記。跟主專案（`圖控軟體開發`）維護 `CLAUDE.md`／`docs/map` 是同一個精神：先讀這頁再動手，省下重新摸索的時間。

## 這個網站是什麼

**MBC 圖控編程** 軟體（另一個獨立的 git 專案，路徑在 `D:\Gemini_Antigravity\圖控軟體開發`）的**對外使用手冊**，用 [Docsify](https://docsify.js.org/) 架站，靠 GitHub Pages 免費託管，**沒有建置步驟**——改完 `.md` 檔存檔、`git push` 就生效，不用跑任何 build 指令。

- Repo：`https://github.com/MBCrobot89/mbc-blockcode-guide`
- 網址：`https://mbcrobot89.github.io/mbc-blockcode-guide/`
- 這個 repo **是公開的**、跟軟體本身的原始碼是分開的兩個專案——不要把軟體的 `src/`、`bin/` 之類的東西複製過來。

## 目錄結構

```
index.html          Docsify 載入器＋設定（很少需要改）
_sidebar.md          左側選單，新增頁面一定要記得在這裡加一行，不然點不到
README.md            首頁
版權聲明.md           著作權與第三方套件授權（改動前**務必**跟公司/使用者確認）
01-快速開始/ ~ 08-老師專屬設定/
                     八大分類，每個資料夾一個 README.md 當該分類的總覽頁
images/              截圖，依章節資料夾分類存放
```

## 內容進度（2026-09-22 更新：一到八章全部寫完）

**八大分類全部寫完**：一、快速開始／二、軟體介面導覽／三、積木操作技巧／四、積木字典／五、自己做積木／六、燒錄與疑難排解／七、專案與檔案管理／八、老師專屬設定，加上著作權聲明。多數頁面都有真實操作截圖（少數天生沒辦法截圖的例外：原生 `<select>` 展開選單、原生「關於」對話框——隱藏視窗點開原生 OS 疊層會讓 renderer crash，這幾處改成截「關閉狀態」＋文字描述）。

之後如果要繼續補內容，是「深化既有章節」（例如 6.5 補更多實際錯誤案例），不是從骨架開始——所有 `README.md` 骨架頁都已經換成有實際連結的目錄頁。

積木字典的截圖／文字內容是這樣來的：flyout 截圖用「隱藏視窗＋`capturePage()`」技巧對每個分類逐一截圖；switch／如果／副程式的 mutator（點齒輪 ⚙️ 展開後的樣子）截圖，是先用 `Blockly.serialization.blocks.append()` 建立空積木、再呼叫 `domToMutation()` 展開分支/參數之後才截圖（mutator 產生的輸入插槽在 `domToMutation` 執行前是不存在的，直接在 `append()` 裡塞值會報錯）；每顆積木的說明文字則是直接從 `src/core.abpk` 裡每顆積木定義的 `tooltip` 欄位取出來的（已經是原本就寫給老師看的白話文字，不用重寫），副程式／C語言兩類的積木不在 `core.abpk`（是 Blockly 內建或 `src/code-generator.js`／`src/block-loader.js` 額外處理的），說明文字是照這幾個檔案裡的註解跟實際行為改寫的。

補內容時：在對應資料夾新增一頁 `.md`（檔名格式參考既有頁面，數字開頭＋標題）、寫完後**一定要去 `_sidebar.md` 加連結**，並且把該分類 `README.md` 骨架頁裡對應那一條的說明文字換成真的連結（模仿 6.7 那頁的做法：規劃中的維持條列，做完的加上 ✅ 並連過去）。

## 截圖是怎麼來的（不是手動截圖）

真實的軟體畫面，用「隱藏視窗＋`capturePage()`」產生，跟主專案在做功能驗證時用的技巧完全一樣：

1. 用 Electron 的 dev 模式（`node_modules/electron/dist/electron.exe`）載入主專案的 `index.html`／`renderer.js`，視窗 `show:false`，畫面上什麼都看不到。
2. 用 `webContents.sendInputEvent()` 模擬真實的滑鼠拖曳／點擊／鍵盤操作（不是直接用程式 API 生積木，能拖曳的步驟盡量真的用滑鼠拖，畫面才會跟老師實際操作時看到的一致）。
3. 每一步用 `webContents.capturePage(rect)` 截圖，`rect` 用 `element.getBoundingClientRect()` 量出來，存成 PNG。
4. 隱藏視窗不會自己畫面更新（`requestAnimationFrame` 不會自動跑），操作之後要**先呼叫幾次 `capturePage({width:1,height:1})` 把畫面「推」出來**，再讀狀態或截正式的圖——這個坑主專案的 `docs/map/F-測試與除錯手法.md` 也記錄過，是同一個问题。

腳本本身是一次性的（`圖控軟體開發/shot-*-tmp.js`），跑完就刪掉，不會留在任一個 git 專案裡——**要補新截圖，照上面的做法重寫一份新的臨時腳本**，不用去找舊的（已經刪了）。

## 寫作規則

- 對象是**完全不懂程式的國小老師**，用詞要白話，不要出現「非同步」「事件監聽」這種術語；程式碼相關名詞第一次出現要順便解釋。
- 每個操作步驟盡量配一張截圖，這是使用者明確要求的（「圖片越多越好」）。
- 每頁最下面放「上一步／下一步」的導覽連結，讓不熟悉網站的老師也能一路往下看完，不用回頭找選單。
- 圖片路徑一律用相對路徑（`../images/...`），不要寫死 GitHub 網址，本機開起來（用 `docsify serve` 或任何本機伺服器）也要看得到圖。
